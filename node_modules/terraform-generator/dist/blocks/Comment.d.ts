import { Argument, Attribute } from '../arguments';
import { Block } from '.';
/**
 * @category Block
 */
export declare class Comment extends Block {
    readonly comment: string;
    /**
     * Construct comment.
     *
     * @param comment comment
     */
    constructor(comment: string);
    toTerraform(): string;
    asArgument(): Argument;
    attr(_name: string): Attribute;
}
